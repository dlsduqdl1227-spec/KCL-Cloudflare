# Stage 108 KCR 스마트태그 최종 검수 보고서

## 1. 기준 자료

- 규정집: `@2026_KCR_룰북(수정완료)@_(260729).pdf`
- 세부 요구사항: `pasted-text.txt`의 KCR 스마트태그 정의
- 적용 범위: KCR 평가, KCR 검수, KCR 순위 집계, KCR 디브리핑
- 비적용 범위: KBC, KTCC, MOB, MOC, KCAC, IKRC의 평가 규칙과 데이터 구조

## 2. 규정 및 요구사항 반영

- 평가 항목 순서를 `Flavor -> Aftertaste -> Acidity -> Sweetness -> Mouthfeel -> Overall`로 통일했다.
- 기존 KCR `Body(바디)` 명칭과 계산 키를 `Mouthfeel(마우스필)`로 교체했다.
- 각 항목 점수는 0.2점 단위를 유지하고 Sweetness만 2배로 합산한다.
- KCR 스마트태그 139개를 별도 데이터 파일로 분리했다.
- 태그 수는 Flavor 71개, Mouthfeel 18개, Acidity 15개, Sweetness 14개, Aftertaste 21개다.
- 화면 표시명과 저장 키를 분리하고 저장값은 고유 ID만 사용한다.
- Flavor는 최대 5개, 나머지 항목은 최대 3개까지 선택할 수 있다.
- Aftertaste의 일반 태그와 Flavor 참조 태그는 합산 최대 3개로 제한한다.
- 태그 선택은 점수 슬라이더에 영향을 주지 않는다.
- 항목별 자동 코멘트와 직접 수정 코멘트를 분리하고, 직접 수정한 코멘트는 태그 변경 시 덮어쓰지 않는다.

## 3. 발견 및 수정한 문제

1. 기존 계층형 태그에서 탐색용 상위 카테고리와 최종 태그가 함께 저장될 수 있었다.
   - 상위 카테고리는 탐색에만 사용하고 최종 태그 ID만 저장하도록 교체했다.
2. 자동 코멘트가 표시명 또는 중간 카테고리를 잘못 받아 문장이 불완전해질 수 있었다.
   - 카테고리별 독립 생성기와 ID 기반 라벨 변환을 적용했다.
3. KCR 평가와 검수에 `Body`와 `Mouthfeel` 키가 혼재해 총점이 다르게 표시될 수 있었다.
   - 저장 헤더, 검수 합계, 순위, 디브리핑을 `Mouthfeel` 기준으로 통일했다.
4. 검수 화면에서 팀장에게 세부 점수 카드가 노출되지 않았다.
   - KCR 담당 팀장만 KCR 세부 검수와 수정이 가능하도록 권한 범위를 보완했다.
5. 새 `Overall(오버롤)` 키가 KCR 동점 기준에서 인식되지 않아 0점으로 표시될 수 있었다.
   - KCR 동점 집계 키를 보완하고 실제 3.0점 반영을 확인했다.
6. 디브리핑에서 비어 있는 항목의 내부 자동 코멘트 상태 JSON이 코멘트로 노출될 수 있었다.
   - KCR 공개 코멘트 수집에서 내부 상태 필드를 제외했다.
7. 모바일 선택 체크 아이콘이 버튼 외부로 나가 일부 안드로이드 화면에서 잘릴 수 있었다.
   - 체크 아이콘을 KCR 태그 버튼 내부에 배치했다.

## 4. 저장 및 계산 검증

- 합성 평가 1건을 제출해 태그 ID, 선택 순서, 점수, 강도, 항목별 코멘트, 코멘트 상태가 같은 점수 레코드에 저장되는 것을 확인했다.
- 검증 점수: Flavor 3.0 + Aftertaste 3.0 + Acidity 3.0 + Sweetness 3.0 x 2 + Mouthfeel 3.0 + Overall 3.0 = 총점 21.0.
- 검수 저장 후에도 총점은 21.0으로 유지됐다.
- 순위 집계는 총점 21.0, 동점 기준 Sweetness 3.0, Overall 3.0으로 확인됐다.
- 디브리핑은 한글 태그명과 Flavor, Aftertaste, Overall 코멘트를 표시하고 내부 상태 JSON은 표시하지 않는다.
- 운영 DB 마이그레이션은 필요하지 않다. 요구사항에 따라 기존 KCR 평가 데이터가 없는 상태를 전제로 신규 구조를 바로 적용한다.

## 5. 테스트 결과

- `npm test`: 통과
  - Stage 107 전체 대회 안전성 테스트
  - Stage 108 KCR 태그, 선택 제한, 코멘트, 저장 헤더, 총점, 동점, 디브리핑 테스트
- JavaScript 문법 검사: 통과
- `git diff --check`: 통과
- 런타임 의존성 보안 검사: 취약점 0건
- 로컬 HTTP 점검: 홈, 평가, 디브리핑, KCR 태그 및 코멘트 자산 모두 HTTP 200
- 모바일 390 x 844 점검: 평가 검수와 디브리핑 모두 가로 넘침 없음
- 브라우저 콘솔 오류: 0건

## 6. 최종 판단

KCR 스마트태그 개편 범위는 배포 가능 상태다. 배포 직전 운영 D1을 백업하고 운영 데이터 건수를 기록한 뒤 GitHub와 Cloudflare Pages에 배포한다. 배포 후 정적 자산, API, 운영 DB 건수 불변 여부를 다시 확인한다.
