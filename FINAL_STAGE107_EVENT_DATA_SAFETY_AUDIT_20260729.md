# KCL Stage107 참가 규모·기록 안전 최종 검수 보고서

- 검수일: 2026-07-29
- 기준 코드: `KCL_STAGE106_IKRC_BLIND_STATION_20260727`
- 결과 코드: `KCL_STAGE107_EVENT_DATA_SAFETY_20260729`
- 참가자 기준 파일: `@@2026_KCL_참가자@@(공유용).xlsx`
- 직접 수정 범위: 참가 규모 대응, 컵 범위 입력, 점수 원자 저장, 계정별 기록 분리, KCAC 사진 저장 안전

## 1. 업무 목표

1. 실제 참가 인원으로 대회 진행과 데이터 저장에 문제가 없는지 확인한다.
2. 대회가 서로 다른 날짜에 진행되는 조건에서 D1 저장 용량과 동시 제출 부하를 확인한다.
3. 다수 컵 대회의 개별 입력과 범위 입력을 유연하게 지원한다.
4. 제출 중 일부 기록만 저장되거나, 다른 심사위원의 기록이 섞여 보이는 위험을 제거한다.
5. KCAC 사진 데이터가 Cloudflare D1 한 행 제한을 넘지 않게 한다.

이번 수정은 7개 대회의 평가 항목, 배점, 감점, 총점, 순위 계산식을 변경하지 않았다.

## 2. 참가 인원 확인

| 대회 | 공유 엑셀 확인 인원 | 운영 판단 |
|---|---:|---|
| KBC | 28명 | 문제 없음 |
| KTCC | 40팀 | 문제 없음 |
| MOB | 55명 | 문제 없음 |
| MOC | 177명 | 문제 없음. 번호 177~179 공백 확인 필요 |
| KCR | 226명 | 문제 없음. 범위를 최대 20개씩 나누어 제출 |
| IKRC | 67명 | 문제 없음 |
| KCAC | 22명 | 문제 없음 |

- 전체 등록 규모는 현재 615명/팀이다.
- KCAC를 현장 계획대로 30명으로 늘려도 전체 623명/팀 수준이다.
- 대회가 같은 시간에 중복되지 않는 조건은 동시 쓰기 부하를 크게 낮춘다.
- 공유 엑셀의 KCAC 현재값은 30명이 아니라 22명이다. 추가 8명은 운영 등록 파일에 반영해야 한다.
- 공유 엑셀에는 참가자 연락처가 없어 디브리핑 로그인용 실제 등록 템플릿을 대체할 수 없다.
- 심사위원은 25명이 확인됐지만 공유 엑셀의 대회 배정 열에는 MOC, KTCC, KCAC 배정이 아직 보이지 않는다. 실제 운영계정 엑셀에서 최종 배정해야 한다.

## 3. 발견한 문제

### 3.1 컵 범위 입력 제한

- KCR 범위 입력이 숫자 중심이라 `A-1`, `AA-01`, `001` 같은 현장 코드를 일관되게 처리하기 어려웠다.
- MOC 등록번호에 공백이 있어 1~180 범위를 그대로 만들면 미등록 177~179가 평가 대상에 포함될 수 있었다.
- 지나치게 큰 KCR 범위를 한 번에 제출하면 네트워크 실패 시 재확인 범위가 커졌다.

### 3.2 KCR 다건 부분 저장 위험

- KCR 여러 컵이 순차 저장되어 중간 항목에서 중복 또는 오류가 발생하면 앞 항목만 남을 수 있었다.
- 네트워크 재전송 시 20초 이후에는 같은 평가가 다시 저장될 수 있었다.

### 3.3 동일 연락처 계정의 기록 혼동

- 같은 전화번호를 공유하는 서로 다른 이름의 운영계정이 있으면 전화번호만으로 구버전 기록 소유권을 판단할 여지가 있었다.
- 새 기록뿐 아니라 기존 테스트 기록도 상대 계정의 검수 목록에 보일 수 있었다.

### 3.4 KCAC 사진 행 크기

- KCAC가 잔별 스냅샷 2개와 큰 이미지를 함께 저장하면 D1의 한 행 2MB 제한에 근접하거나 초과할 수 있었다.
- 이 위험은 참가자 수와 무관하므로 22명 또는 30명이어도 동일하게 차단해야 했다.

### 3.5 결선 블라인드 번호 재사용

- KCR/IKRC 결선 번호가 없을 때 예선 번호로 자동 대체되면 잘못된 선수에게 점수가 연결될 수 있었다.

## 4. 완료한 수정

### 4.1 유연한 컵 번호와 범위

- 숫자, `001`, `A-1`, `AA-01` 형식을 지원한다.
- 같은 접두어 안에서만 범위를 생성한다. `A-1~B-1`은 거부한다.
- KCR은 한 번에 최대 20개 컵으로 제한한다.
- KCR/MOC는 참가자 등록 목록이 로드되면 미등록 번호를 범위에서 자동 제외한다.
- MOC 1~180 범위에서는 현재 명단 기준 177, 178, 179가 제외된다.
- 비상 개별 입력은 미등록 번호임을 명확히 확인한 뒤에만 허용한다.
- IKRC 스테이션 코드는 최대 8자로 맞춰 프런트와 서버 규칙을 통일했다.

### 4.2 원자 저장과 중복 방지

- KCR과 IKRC 다건 제출을 D1 `batch()`로 처리한다.
- 제출 묶음 안에 중복 또는 잘못된 항목이 하나라도 있으면 전체 묶음을 저장하지 않는다.
- KCR 중복 기준을 대회, 라운드, 역할, 컵, 프로세스, 켈리브레이션 구분, 인증 계정으로 확인한다.
- KCR 재전송은 시간과 관계없이 차단하고 검수 화면에서 수정하도록 안내한다.

### 4.3 계정별 기록 분리

- 인증된 이름과 전화번호를 조합한 `operatorIdentityKey`를 새 점수에 저장한다.
- 같은 전화번호를 쓰더라도 이름이 다른 계정은 서로의 기록을 볼 수 없다.
- 구버전 기록은 저장된 심사위원 이름이 있으면 이름을 전화번호보다 먼저 확인한다.
- KBC 중복 판정도 단순 심사위원명 SQL 비교 대신 인증 계정 소유권을 확인한다.

### 4.4 KCAC 사진 저장 제한

- 잔별 서버 저장 스냅샷을 최신 1개로 제한한다.
- 전체 이미지는 최대 120KB, 썸네일은 최대 18KB로 압축한다.
- 브라우저 임시 보관도 최신 3개만 유지한다.
- 서버는 JSON payload가 1,750,000바이트를 넘으면 저장 전 거부한다.
- 최악 조건 3잔 사진 payload 실측은 424,978바이트였다.

KCAC 30명이 모두 3잔 사진을 최대 크기로 저장해도 약 12.8MB 수준이다. Cloudflare D1 공식 한도는 Free 데이터베이스 500MB, Paid 데이터베이스 10GB이며 한 행 제한은 2MB이므로 현재 보수적 제한 안에서 30명 운영은 가능하다.

### 4.5 기타 기록 안전

- KCR/IKRC 결선은 결선 블라인드 번호가 없으면 예선 번호를 재사용하지 않는다.
- 로그인 시 만료된 세션을 기회 정리해 장기 운영 시 세션 테이블 누적을 줄인다.
- 점수와 로그인 템플릿 구조, 기존 `public/assets` 파일은 변경하지 않았다.

## 5. 수정한 파일

### `functions/api/rpc.js`

- `operatorIdentityKey_`
- `scoreOwnedByActor_`
- `participantRoundNumber_`
- `utf8ByteLength_`
- `kcrProcessKeyFromPayload_`
- `submitScores`
- `itemJudgeIdentityKey_`
- `issueSession`

### `public/assessment/index.html`

- 컵 번호 정규화·비교·범위 생성 함수
- KCR/MOC 등록 참가자 범위 필터
- KCR 최대 20개 제출
- KCAC 사진 압축·최신 스냅샷 제한
- IKRC 스테이션 접두어 길이 통일

### `package.json`, `tests/stage107-safety.test.mjs`

- Stage107 안전 회귀 테스트 추가

## 6. 검증 결과

| 검증 항목 | 결과 |
|---|---|
| `rpc.js` 문법 검사 | 통과 |
| 5개 HTML 인라인 스크립트 문법 검사 | 통과 |
| `AA-01~AA-03`, `001~003` 범위 | 통과 |
| 서로 다른 접두어 범위 거부 | 통과 |
| KCR 21개 초과 범위 거부 | 통과 |
| MOC 177~179 미등록 번호 제외 | 통과 |
| KCR 2개 컵 원자 저장 | 통과 |
| KCR 신규+중복 혼합 묶음 전체 취소 | 통과 |
| KCR 영구 중복 제출 차단 | 통과 |
| 같은 전화번호·다른 이름의 새 기록 분리 | 통과 |
| 같은 전화번호·다른 이름의 구기록 분리 | 통과 |
| KCAC 사진 포함 3행 저장·조회 | 통과 |
| KCAC 1.75MB 초과 저장 차단 | 통과 |
| KCR 심사 기록 678건 저장 후 전체 조회 | 통과 |
| KCR 검수 목록 680건 조회 | 144ms |
| 17개 동시 제출 × 20행, 총 340행 | 전부 저장, 982ms |
| 1,020건 KCR 본인 검수 목록 조회 | 171ms |
| 412×915 모바일 KCR 로그인·범위·평가 화면 | 통과 |
| 모바일 가로 넘침·헤더 겹침 | 발견 없음 |

성능 수치는 로컬 Wrangler/D1 측정값이며 원격 네트워크 지연을 포함하지 않는다. 다만 실제 최대 참가 수보다 보수적으로 기록 수를 늘리고 17개 동시 제출을 수행해 데이터 규모 자체가 병목이 아님을 확인했다.

## 7. 운영진 확인 필요

1. KCAC 추가 참가자를 포함해 최종 인원이 22명인지 30명인지 확정하고 실제 참가자 등록 엑셀에 반영한다.
2. MOC 번호 177, 178, 179가 의도한 결번인지 확인한다.
3. MOC, KTCC, KCAC 심사위원 역할과 팀 배정을 실제 운영계정 엑셀에서 완료한다.
4. 참가자 디브리핑용 등록 파일에는 이름과 연락처가 모두 있어야 한다.
5. KCR/IKRC 결선 시작 전 결선 블라인드 번호를 반드시 배정한다.
6. 같은 전화번호를 여러 운영계정이 공유할 수는 있지만 현장 혼동을 줄이려면 가능하면 개인 번호로 분리한다.

## 8. 실제 대회 전 필수 체크

1. 원격 D1 전체 SQL 백업과 현재 Time Travel bookmark 확보
2. 현재 운영 배포본 ZIP 또는 Git 커밋 백업
3. 참가자 등록 인원과 실제 출전 인원 대조
4. 운영계정 이름, 연락처, 대회, 역할, 팀 배정 대조
5. 각 대회 일반 심사위원 계정으로 로그인
6. 본인 평가 1건 제출 후 본인 검수 목록에만 보이는지 확인
7. 팀장 계정에서 전체 점수와 코멘트 확인
8. 테스트 점수만 해당 대회·라운드에서 초기화
9. 점수·검수 백업 엑셀 다운로드 후 행 수 확인
10. Android, iPhone, 태블릿에서 제출 리허설
11. Cloudflare D1 Metrics에서 저장 용량, 읽기, 쓰기, 오류 확인
12. 대회 중 각 세션 종료 직후 팀장 점수 백업 다운로드

## 9. 배포 전 백업 목록

- Cloudflare 원격 D1 전체 데이터베이스
- `competitions`
- `operators`
- `participants`
- `scores`
- `sessions`
- `otps`
- `sms_logs`
- `rate_limits`
- `security_events`
- `public/assets`의 선수·운영계정 원본 엑셀
- 현재 운영 배포본 전체 폴더
- Cloudflare 환경 변수·Secret 이름 목록
- 대회별 점수·검수 백업 엑셀

Cloudflare D1 Time Travel은 자동 활성화되며 Free는 최근 7일, Paid는 최근 30일 복구를 지원한다. Time Travel만 의존하지 말고 대회 전후 SQL/엑셀 백업도 별도로 보관한다.

## 10. 배포 안내

- DB 마이그레이션 추가 없음
- `.wrangler`, 로컬 로그, 합성 테스트 데이터는 최종 폴더와 ZIP에서 제외
- GitHub/Cloudflare에는 Stage107 전체 프로젝트 구조를 기준으로 반영
- `public`만 업로드하지 말고 `functions`, `migrations`, `wrangler.toml`, `package.json`을 함께 유지
- 배포 직후 `/api/rpc`의 `ping`, 운영계정 로그인, 테스트 1건 제출·조회까지 확인

## 11. 최종 판단

**조건부 배포 가능**

KCAC 30명, KCR 226명 등 현재 참가 규모는 저장 용량과 조회 성능에 문제가 없다. 점수 부분 저장, 장기 중복 제출, 동일 전화번호 계정 혼동, KCAC 2MB 행 초과 위험을 보완했고 로컬 API·동시 제출·모바일 검증을 통과했다.

배포 전 필수 조건은 원격 D1 백업, KCAC 최종 인원 반영, MOC 결번 확인, 미완료 심사위원 배정, 실제 운영 계정과 현장 네트워크로 1회 리허설이다.

## 12. 근거 링크

- Cloudflare D1 Limits: https://developers.cloudflare.com/d1/platform/limits/
- Cloudflare D1 Time Travel: https://developers.cloudflare.com/d1/reference/time-travel/
