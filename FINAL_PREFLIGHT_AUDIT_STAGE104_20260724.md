# 2026 KCL 디지털 평가 시스템 대회 직전 최종 점검 보고서

- 점검일: 2026-07-24
- 최종 코드: `KCL_STAGE104_REGISTRY_EXPORT_20260724`
- 대상: KBC, KTCC, MOB, MOC, KCR, KCAC, IKRC
- 최종 판단: **조건부 배포 가능**

로컬 빈 D1에서 7개 대회 공통 API 흐름과 모바일 화면을 다시 검증했다. Stage104는 Stage103의 점수·라운드·권한 안정화를 유지하면서, 화면에서 등록한 선수·심사위원·팀장을 엑셀로 다시 내보내는 기능을 추가한 버전이다.

## 1. 기준 자료

NAS 경로 `\\LG-NAS\Multimedia\%%2026_KCL%%\1. 대회 운영 관련`를 2026-07-24에 다시 조회했다. 2026-07-10 이후 더 최신인 대회 규정집은 확인되지 않았다.

| 대회 | 최종 기준 규정집 |
|---|---|
| KBC | `2026_KBC_룰북(0624 공식원두 업데이트).pdf` |
| KTCC | `2026 KTCC_룰북(0630 updated).pdf` |
| MOB | `2026 MOB 룰북(26.05.18 수정).pdf` |
| MOC | `2026 MOC_룰북(26.05.18 수정).pdf` |
| KCR | `2026_KCR_룰북(0618_updated).pdf` |
| KCAC | `KCAC_2026_규정집(07.10).pdf` |
| IKRC | `2026_IKRC_ 룰북.pdf` |

공통 운영 결정도 유지했다.

- 평가 점수 입력 단위: 0.2점
- 예선, 본선, 결선 디브리핑: 운영진 설정 시 모두 공개 가능
- 켈리브레이션: 실제 평가·순위·공식 백업 탭과 분리
- MOC/KTCC 정답 개수: 0.2점 입력 대상이 아닌 정수형 경기 데이터

## 2. 이번에 발견하고 수정한 문제

### 중요: 제출자 신원과 역할을 클라이언트 값으로 저장할 수 있었음

수정 전에는 오래된 화면이나 변조된 요청이 `judgeName`, `judgeRole`, `team`을 임의로 보내면 그 값이 점수 payload에 남을 수 있었다. 이 경우 심사위원별 검수 목록, 역할 표시, 중복 제출 판정이 실제 로그인 계정과 어긋날 가능성이 있었다.

수정 후에는 `submitScores()`가 유효한 로그인 세션의 이름, 전화번호, 대회별 역할, 대회별 팀을 서버에서 다시 확정한다. 클라이언트가 다른 심사위원 이름·역할·팀을 보내도 저장값은 인증된 계정 기준으로 고정된다. MOB 센서리/테크니컬 본인 검수 격리도 이 서버 기준으로 동작한다.

### 중요: 점수 payload에 인증 토큰이 남을 수 있었음

수정 전에는 제출 요청의 `judgeToken`, `actorToken`, 중첩된 토큰이 `payload_json`에 저장될 가능성이 있었다. 관리자 백업이 이 값을 포함하면 유효기간이 남은 세션 토큰과 연락처가 결과 파일에 노출될 수 있었다.

수정 후에는 저장 전에 모든 인증 토큰을 제거하고, 백업·보고서 생성 시에도 재귀적으로 토큰, 비밀번호, secret 값, 심사위원·운영자 연락처를 제거한다. DB의 제출 소유권 판정에 필요한 정식 필드는 유지한다.

### 등록된 계정이 엑셀 템플릿에 표시되지 않았음

수정 전 `엑셀 템플릿 다운로드`는 `/assets`에 있는 고정 빈 양식만 내려받아, 화면에서 등록한 선수·심사위원·팀장 정보가 보이지 않았다. 이는 등록 데이터 유실이 아니라 빈 가져오기 양식과 현재 데이터 내보내기의 구분이 없었던 UI 문제였다.

수정 후 `빈 엑셀 양식`과 `현재 등록자료 엑셀`을 분리했다. 현재 등록자료는 다운로드 직전에 D1을 다시 조회하고 `작성가이드`, 7개 `_선수`, 7개 `_운영계정` 탭으로 내보낸다. 관리자 계정은 보안상 제외하며 `ALL`·복수대회 권한 계정은 한 번만 기록하고 원래 권한 문자열을 보존한다.

## 3. 수정 파일과 영향 범위

| 파일 | 수정 내용 | 영향 범위 |
|---|---|---|
| `functions/api/rpc.js` | `submitScores()` 인증 계정 기준 신원·역할·팀 고정, 저장 전 토큰 제거 | 7개 대회 제출 공통 경로 |
| `functions/api/rpc.js` | `stripPayloadForReport_()` 토큰·비밀번호·연락처 재귀 제거 | 관리자 백업·보고서 출력 |
| `public/registry/index.html` | 빈 양식과 현재 등록자료 엑셀 분리, D1 등록 현황 15개 탭 내보내기 | 등록관리 화면만 해당 |
| `FINAL_PREFLIGHT_AUDIT_STAGE104_20260724.md` | 최종 점검 결과 갱신 | 문서만 해당 |

`public/assessment/index.html`은 Stage102와 SHA-256이 동일하다. 따라서 기존 Android 포커스 안정화, MOB 자동 코멘트, 스마트태그, 점수·라운드 UI는 변경하지 않았다. `rpc.js`의 나머지 278개 함수도 Stage102와 동일하다. Stage104에서 새로 변경한 화면은 등록관리 페이지뿐이다.

## 4. 대회별 최종 상태

| 대회 | 라운드·역할·점수 격리 | 저장·검수·순위·백업 | 상태 |
|---|---|---|---|
| KBC | 예선/본선/결선 및 역할별 화면 유지, 0.2점·감점·총점 로직 유지 | 제출, 본인 검수, 관리자 검수, 중복 제출 방지, KBC 단독 초기화 통과 | 통합 회귀 통과 |
| KTCC | 팀 단위 예선/결선 및 정답·시간 데이터 분리 유지 | 팀 점수, 순위, 백업, KTCC 단독 데이터 경로 통과 | 통합 회귀 통과 |
| MOB | 센서리/테크니컬 화면 분리, 각 역할의 본인 제출만 조회 | 센서리 18점+테크니컬 9점=27점 집계, 검수완료·순위 반영 통과 | 모바일 포함 통과 |
| MOC | 예선/본선/결선, 정답·가산점·시간 데이터 분리 유지 | 제출, 검수, 순위, 백업, 대회별 격리 통과 | 통합 회귀 통과 |
| KCR | 예선/결선·켈리브레이션 분리, 스마트태그·강도 문구 유지 | 제출, 검수, 순위, 백업, 대회별 격리 통과 | 기능 통과, 정규화 운영값 확인 필요 |
| KCAC | 온라인 예선/현장 결선, 평가·사진·감점 구조 유지 | 제출, 검수, 순위, 백업, 대회별 격리 통과 | 기능 통과, 실제 미디어 권한 확인 필요 |
| IKRC | 예선/결선·켈리브레이션·Seed to Cup 분리 유지 | 제출, 검수, 순위, 백업, 대회별 격리 통과 | 기능 통과, Technical Time 절차 확인 필요 |

## 5. 테스트 결과

### 정적 검사

- 배포 파일 26개 기준 검사 통과
- HTML 6개, 인라인 스크립트 5개, JS 3개 문법 통과
- 중복 정적 ID 0건
- PNG 자산 및 경로 검사 통과
- CSP, `no-store`, 정적 로고 immutable 캐시 헤더 확인
- QA 계정·전화번호·토큰 패턴 배포 파일 유입 0건
- XLSX 등록 템플릿 구조 정상
- 현재 등록자료 XLSX: 15개 탭 생성, 선수·팀·운영계정 값과 연락처 문자열 보존 확인
- 운영계정: `ALL` 권한과 팀장 권한을 각각 한 번만 기록, 관리자 계정 제외 확인
- 현재 등록자료 XLSX 재업로드: 선수·팀 2건, 운영계정 2건을 다시 가져와도 DB 건수 불변(멱등 처리) 확인

### 로컬 D1 통합 검사

빈 로컬 D1에 마이그레이션 `0001`~`0005`를 적용한 후 합성 계정·참가자로 실행했다.

- `/api/health`: 통과
- 심사위원·관리자 직접 로그인: 통과
- 7개 대회 참가자 목록·배정 조회: 7/7 통과
- 인증 계정 기준 제출자 이름·역할·팀 고정: 통과
- MOB 센서리/테크니컬 본인 검수 격리: 통과
- MOB 관리자 검수완료 및 총 27점 순위 반영: 통과
- 백업의 토큰·연락처 제거: 통과
- 7개 대회 제출→검수→순위→백업 흐름: 7/7 통과
- KBC 동일 요청 재시도 멱등 처리와 변경 중복 제출 차단: 통과
- KBC 초기화 후 MOB 데이터 유지: 통과
- 저장된 점수 payload의 인증 토큰 패턴: 0건

### 360x800 모바일 화면 검사

- MOB 센서리 계정: 센서리 6개 항목만 표시, 테크니컬 항목 미표시
- MOB 테크니컬 계정: 테크니컬 3개 항목만 표시, 센서리 항목 미표시
- 테크니컬 본인 검수: 전체 1건만 표시
- 참가자명 입력에 커서가 남은 상태에서 하단 점수 슬라이더 클릭: 스크롤 이동 0px
- 가로 오버플로: 0px
- MOB 자동 코멘트: 초안 3개 생성, 선택 후 입력란 반영, 직접 수정 경고 정상
- 브라우저 warn/error 로그: 0건

### 로컬·운영 주소 확인

- 로컬 `/`, `/assessment/`, `/admin/`, `/registry/`, `/debriefing/`, `/camera/`, `/api/health`: 모두 HTTP 200
- 운영 `https://kcl-cloudflare.pages.dev/`, `/assessment/`, `/api/health`: 모두 HTTP 200
- 운영 평가 HTML과 Stage104 평가 HTML: SHA-256 정확히 일치

운영 평가 HTML은 동일하지만 Stage103 이후의 서버 보안 변경과 Stage104 등록자료 엑셀 기능은 이 패키지를 새로 배포한 뒤 반영된다.

## 6. 남은 버그와 운영진 확인 필요 항목

현재 로컬 재현 기준으로 배포를 막는 미수정 코드 버그는 발견되지 않았다. 아래는 현장 데이터·외부 서비스·운영 결정이 필요한 항목이다.

1. **Cloudflare 관리자 비밀값**: `KCL_ADMIN_SECRET_CODE`, `KCL_ADMIN_PASSWORD`를 Pages 환경변수/Secret으로 설정하고 실제 관리자 로그인을 확인한다. 기존 호환 로그인 방식은 삭제하지 않았다.
2. **원격 D1**: 배포 전 전체 백업 후 마이그레이션 `0001`~`0005` 적용 상태를 확인한다.
3. **실제 엑셀 등록**: 선수등록·운영계정등록 실제 파일로 성공/실패 건수와 역할·팀 매핑을 확인한다.
4. **SMS/OTP**: 실제 SOLAPI 계정으로 1건만 발송하고 OTP 인증·디브리핑 조회까지 확인한다. 문자 기능 코드는 이번에 변경하지 않았다.
5. **KCR 정규화**: 공식 정규화 값 산출 책임자와 입력·승인 절차를 현장에서 확정한다. 임의 공식을 추가하지 않았다.
6. **IKRC Technical Time**: 장비 문제/개인 장비 문제, 승인/불승인, 타이머 정지·재개·재시연 기록 절차를 확정한다.
7. **KTCC 결선 주제·대진**: 비공개 주제와 최종 대진 입력 책임자를 확정한다.
8. **KCAC 미디어**: 실제 영상 URL 권한, 사진 촬영·회전·업로드를 대회용 Android/iPhone에서 각 1회 확인한다.
9. **PDF·XLSX**: 행사장 네트워크와 프린터에서 최종 결과, 백업 엑셀, 디브리핑 PDF를 각 1회 출력한다.

## 7. 실제 대회 전 체크리스트

### 배포 직전

- [ ] 현재 전체 배포 폴더와 ZIP의 SHA-256 기록
- [ ] 원격 D1 전체 SQL 백업 및 복원 파일 열기 확인
- [ ] Cloudflare D1 바인딩 이름 `DB` 확인
- [ ] 마이그레이션 `0001`~`0005` 적용 확인
- [ ] 관리자 Secret/환경변수와 SOLAPI Secret 존재 확인
- [ ] Stage104 전체 폴더를 배포하고 `/api/health` HTTP 200 확인

### 대회 시작 전

- [ ] 관리자, 센서리, 테크니컬, 헤드, 운영계정 각 1회 로그인
- [ ] 7개 대회 참가자·팀·컵번호·라운드 배정 확인
- [ ] 역할별 시험 제출 1건과 본인 검수 목록 격리 확인
- [ ] 관리자 점수 0.2점 수정 후 검수완료·순위 즉시 반영 확인
- [ ] 켈리브레이션 제출이 실제 순위에서 제외되는지 확인
- [ ] 백업 엑셀의 켈리브레이션/예선/본선/결선 탭 구분 확인
- [ ] SMS/OTP 1건과 디브리핑 공개 범위 확인
- [ ] 시험 데이터는 대회별 초기화로 삭제하고 다른 대회 건수 유지 확인

### 대회 중

- [ ] 첫 제출 직후 참가자, 심사위원, 역할, 라운드, 총점 대조
- [ ] 라운드 종료마다 D1 SQL 백업과 관리자 백업 엑셀 저장
- [ ] 공식 발표 전 검수완료 건만 순위에 반영됐는지 확인
- [ ] 실격 사유, 시간, 감점 근거를 원본 기록과 대조
- [ ] 실제 점수 생성 후 전체 초기화 사용 금지

## 8. 배포 전 백업 대상

### 파일

- 현재 전체 배포 폴더와 ZIP
- `wrangler.toml`, `package.json`
- `functions/`, `public/`, `migrations/` 전체
- 선수등록 원본 엑셀과 운영계정등록 원본 엑셀
- 대회별 백업 엑셀, 최종 결과, 디브리핑 PDF
- Cloudflare 환경변수 이름 목록과 배포 ID. Secret 값은 별도 암호화 보관하고 문서에 평문으로 기록하지 않는다.

### D1

```powershell
npx.cmd wrangler d1 export kcl-cloudflare --remote --output .\backup\kcl-cloudflare_YYYYMMDD_HHMM.sql
```

필수 테이블: `competitions`, `operators`, `participants`, `scores`, `sessions`, `otps`, `sms_logs`, `rate_limits`, `security_events`.

백업에는 개인정보와 인증 기록이 포함되므로 접근권한을 제한하고 메신저 공유를 금지한다.

## 9. 최종 판단

**조건부 배포 가능**이다.

코드·로컬 D1·360px 모바일 회귀 기준으로 즉시 수정이 필요한 재현 버그는 없다. Stage104를 전체 배포하고 원격 D1 백업, 관리자 Secret, 실제 등록 엑셀, SMS/OTP, KCR 정규화, IKRC Technical Time을 현장 계정으로 확인하면 실제 대회 운영 가능 상태로 전환할 수 있다.
